make use of the directory structure to correctly layout & functionality.

page layout has consist of 
1. search (search-bar, sort & filter otpions, suggested search items etc...)
2. sidebar (categories, brands, etc...)
3. results (subcategories grid & products grid, pagination etc...)

design basic skeleton layout for the page while data is loading.
create the above component in suitable directory.
this page should have a state management to handle the search, filter, sort, pagination etc... 
these parameters should also managed through URL Query String parameters.
these parameters should be used to fetch the data from the by react query - just design a basic query - do not need to call the apis (keep it commented).
a global state management should be used to manage larger states like user state & enquiry state.

Keep the code clean & readable & type-safe. Keep things organized & separated.

---

refactor components
item-card (& item-card-grid in the same file)
sub-category-card ($ sub-category-card-container in the same file (single row with carousel or horizontal scroll))
suggested-searches (& suggested-search inside the same file.)

data fetched from the server should be used from store (global states) instead of passing through props.
browse should have a type param values either PRODUCT or SERVICE.
enquiry state should be syncd with localstorage.

new functionality/component
on click of "add to enquiry" (button in the item-card), opens a modal-compoment (create component) with necessary fields (quantity, remarks etc...) & upon "add to enquiry" (button in the modal) it updates the enquiry state.

---

