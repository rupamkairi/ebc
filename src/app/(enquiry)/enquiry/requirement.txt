You need to implement & hook up the enquiry creation pages & implement the existing enquiry state from /store

You need to make following routes.
page: /create 
page: /create/otp-verify
page: /create/review-submit
page: /create/submit-success

0. page /create should consist of following layouts/components.
1. enquiry details (fields [name, email, phone, address, pincode, description, purpose, attachments]  make complex form elements their separate component.) (should automatically fetch the user details)
2. enquiry line_items (this will be a table, make each rows a component)
3. add_item (may wanna reuse src/components/search/item-search/item-search.tsx - to use make a wrapper or instead of directly touching the component.) and a button that takes back to browse page
4. "next" button takes to /create/otp-verify page where user would be asked to enter otp (after successful verification move to remove this file from route history)
5. after verification user should be taken to /create/review-submit page where user would be asked to review and submit the enquiry
6. based on the submission success or failure user should be taken to /create/submit-success or back to /create page where user can see the error & hints.

this page should seem like public page - but based on the logged in user state functionalities may vary.
hook this page with "add to enquiry" of src/components/browse/add-to-enquiry-modal.tsx

Keep the code clean & readable & type-safe. Keep things organized & separated.
--- 