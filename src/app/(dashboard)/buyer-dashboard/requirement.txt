there will be following pages under this section. (prefix = "/buyer-dashboard")
0. / (landing page for the buyer dashboard as user logs in)
1. /enquiries (List of all previously created enquiries, along with status & updates)
2. /appointments (List of all previously created appointments, along with status)
3. /settings (Buyer profile settings)

components at "/" (please checkout <DashboardContainerCard .../> & <DashboardCard .../> from ...)
1. Rooms (Section) contains a few Room (Card) clicking them leads to "/browse" page with few category pre-selected.
2. Activity (Section) contains 2 Cards (Enquiries & Appointments) - which leads to "/enquiries" & "/appointments" pages respectively.
3. Conference Hall (Section) contains a few Cards (Info, Meetings, Forum, Demos, Live) with few metrics shown.
4. Settings (Section) contains a few 1 Card that leads to "/settings" page. There user can update their known informations.
5. AI Calculator (Section) contains a description & a Chat like Input-Box (make a simple component - do not fancy it).

Some Visual guide
1st Row for (1)
2nd Row for (2)
3rd Row for (3,4)
4th Row for (5)

components at /enquiries & /appointments
List with a summary of the enquiry/appointment with few metrics (like status, date, etc.) shown.
Chips/Badges for Filter or Sort option for so that can apply filters or sort options. such as Pending, Approved, etc... 

component at "/settings"
a few forms grouped by 
1. User Personal details
2. Company details
3. Company Members

