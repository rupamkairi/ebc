You need to implement & hook up the appointment creation pages & implement the existing appointment state from /store

You need to make following routes.
page: /create 
page: /create/otp-verify
page: /create/review-submit
page: /create/submit-success

0. page /create should consist of following layouts/components.
1. appointment details (fields [name, email, phone, address, pincode, description, purpose, attachments]  make complex form elements their separate component.) (should automatically fetch the user details) (you can reuse some components from enquiry page, make sure you are not duplicating too much code.)
2. appointment line_item (a table with single item, make a component out of it.)
3. appointment must have 3 time slots (may wanna create a new component (more on it below...) - to use make a wrapper or instead of directly touching the component.) and a button that takes back to browse page
4. "next" button takes to /create/otp-verify page where user would be asked to enter otp (after successful verification move to remove this file from route history)
5. after verification user should be taken to /create/review-submit page where user would be asked to review and submit the appointment
6. based on the submission success or failure user should be taken to /create/submit-success or back to /create page where user can see the error & hints.

this page should seem like public page - but based on the logged in user state functionalities may vary.
hook this page with "add to appointment" of src/components/browse/add-to-appointment-modal.tsx

Keep the code clean & readable & type-safe. Keep things organized & separated.

--- 

time-slot component
  this is a date & time-range picker component.
  you need pick a date & pick time-slots for that date. (picking date range in unlikely in our usecase)
  time slots have a restriction that can not be more than 4 hours.